import { StyleSheet } from "react-native";

export const mainAppStyle = StyleSheet.create({
    appContainer: {
        justifyContent: 'center',
        alignItems: 'center'
    },
    heading: {
        marginTop: 30,
        fontSize: 24,
        fontWeight: '600',
        letterSpacing: 0.5,
        fontFamily: "Cochin"
    }
})